# Application properties
questionnaire:
  filename: questions

spring:
  messages:
    basename: message
    encoding: utf-8
    fallback-to-system-locale: false
  web:
    locale: ru
  shell:
    interactive:
      enabled: false